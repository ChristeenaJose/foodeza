<?xml version="1.0" encoding="UTF-8"?>
<!-- IIS (URL Authorization module): forbid HTTP access to this folder. PHP still reads orders.json from disk. -->
<configuration>
  <system.webServer>
    <security>
      <authorization>
        <deny users="*" />
      </authorization>
    </security>
  </system.webServer>
</configuration>
